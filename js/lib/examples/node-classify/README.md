# node-classify

Classify a Beat Saber map from raw note data using `onnxruntime-node` (native addon).

> **Note:** StackBlitz is not supported — native `.node` addons can't run in WebContainers. Run locally instead.

```bash
pnpm install
node index.mjs
```
