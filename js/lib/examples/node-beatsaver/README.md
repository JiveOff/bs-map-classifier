# node-beatsaver

Classify a BeatSaver map from Node.js using `onnxruntime-node` (native addon).

> **Note:** StackBlitz is not supported — native `.node` addons can't run in WebContainers. Run locally instead.

```bash
pnpm install
node index.mjs
```
